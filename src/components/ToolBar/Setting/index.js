import React from 'react';
import styles from './index.module.less';

export default (props) => {
  return (
    <div className={styles.setting}>
        <i className="iconfont icon-setting-fill"></i>
    </div>
  )
}
